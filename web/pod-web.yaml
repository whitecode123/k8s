apiVersion: apps/v1
kind: Deployment
metadata:
  name: web 
  labels:
    app: web
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web  
  template: 
    metadata:
      labels:
        app: web  
    spec:
      containers:
      - name: web-container  
        image: gcr.io/kdt1-finalproject/njs-web:web-4b790fc74f798ade8ef4cb2b6f3319277c177bbb
        resources:
          requests:
            cpu: "20m"
          limits:
            cpu: "200m"
        ports:
        - containerPort: 80
        volumeMounts:
        - name: apache-config
          mountPath: /etc/apache2/sites-available/000-default.conf
          subPath: 000-default.conf
      volumes:
      - name: apache-config
        configMap:
          name: apache-proxy-config
