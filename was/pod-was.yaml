apiVersion: apps/v1
kind: Deployment
metadata:
  name: was
  labels:
    app: was
spec:
  replicas: 3
  selector:
    matchLabels:
      app: was
  template:
    metadata:
      labels:
        app: was
    spec:
      serviceAccountName: petclinic-ksa
      containers:
      - name: was-container
        image: gcr.io/kdt1-finalproject/njs-was:was-5c679a77b635a99a5c6b6cc2868772166a95c31d
        resources:
          requests:
            cpu: "100m"
          limits:
            cpu: "500m"
        ports:
        - name: http
          containerPort: 8080
      
      - name: cloud-sql-proxy 
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.17.1
        resources:
          requests:
            cpu: "100m"
          limits:
            cpu: "500m"
        args:
        - "--structured-logs"
        - "--port=3306"
        - "--private-ip" 
        - "kdt1-finalproject:asia-northeast3:njs-db"
