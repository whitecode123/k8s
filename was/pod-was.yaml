apiVersion: apps/v1
kind: Deployment
metadata:
  name: was
  labels:
    app: was
spec:
  replicas: 1
  selector:
    matchLabels:
      app: was
  template:
    metadata:
      labels:
        app: was
    spec:
      serviceAccountName: petclinic-ksa
      containers:
      - name: was-container
        image: gcr.io/kdt1-finalproject/njs-was:was-f4f9e4218fe56e8a1dfbc594c23499f62d1717ae
        resources:
          requests:
            cpu: "300m"
          limits:
            cpu: "600m"
        ports:
        - name: http
          containerPort: 8080
      
      - name: cloud-sql-proxy 
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.17.1
        resources:
          requests:
            cpu: "300m"
          limits:
            cpu: "600m"
        args:
        - "--structured-logs"
        - "--port=3306"
        - "--private-ip" 
        - "kdt1-finalproject:asia-northeast3:njs-db"
